<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
    <h3>Title</h4>
    <%= f.text_field :title %>

    <h3>Body</h4>
    <%= f.text_area :body %>

    <%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Show", book_path(:id) %>

<%= link_to "Back", new_book_path %>